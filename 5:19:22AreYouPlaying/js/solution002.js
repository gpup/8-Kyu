//This is the best I've got. Not sure how to go about this

function areYouPlayingBanjo(name) {
    return name.toLowerCase(name.charAt(0) === "r" ? `${name} plays banjo` : `${name} does not play banjo`)
 }