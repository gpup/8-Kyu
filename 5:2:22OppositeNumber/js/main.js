//Very simple, given an integer or a floating-point number, find its opposite.

function opposite(number) {
    //should be easy
    return -number;
  }

  //My first one didn't work but was way over thinking it