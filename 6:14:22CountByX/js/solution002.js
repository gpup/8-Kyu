// was really close, on the right track, i just get confused when trying to apply iterations practically

function countBy(x, n) {
    let z = [];
    for (i = 1; i <= n; i++) {
        z.push(x * i);
    }
    return z;
}